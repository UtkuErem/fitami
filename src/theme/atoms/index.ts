// Export all atomic style elements
// Following atomic design principles

import colors from './colors';
import typography from './typography';
import spacing from './spacing';

export {
  colors,
  typography,
  spacing,
};

export default {
  colors,
  typography,
  spacing,
};
